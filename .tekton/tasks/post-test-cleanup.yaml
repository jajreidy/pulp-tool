---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: post-test-cleanup
spec:
  workspaces:
    - name: repository
  stepTemplate:
    env:
      - name: PULP_CLI_CONFIG
        value: /etc/pulp-access/cli.toml
      - name: PULP_TOOL_PATH
        value: $(workspaces.repository.path)/source
  steps:
    - name: post-test-cleanup
      image: registry.fedoraproject.org/fedora:45@sha256:d0e6bea60fa54e903bf4b6cc800981702774dd6aac7d88f0d3e5a8b518c506bc
      script: |
        #!/usr/bin/env sh
        set -euo pipefail
        dnf install -y python3 pulp-cli

        "${PULP_TOOL_PATH}/e2e/post-test-cleanup.py" --config "${PULP_CLI_CONFIG}"

      volumeMounts:
        - name: cli-toml-volume
          mountPath: /etc/pulp-access
          readOnly: true
  volumes:
    - name: cli-toml-volume
      secret:
        secretName: pulp-access
        items:
          - key: cli.toml
            path: cli.toml
