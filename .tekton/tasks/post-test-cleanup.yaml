---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: post-test-cleanup
spec:
  workspaces:
    - name: repository
  stepTemplate:
    env:
      - name: PULP_CLI_CONFIG
        value: /etc/pulp-access/cli.toml
      - name: PULP_TOOL_PATH
        value: $(workspaces.repository.path)/source
  steps:
    - name: post-test-cleanup
      image: registry.fedoraproject.org/fedora:45@sha256:10593dd8683e738d0d9ef53ae3af57c6940ac02efcb5a104dc4655390dc64698
      script: |
        #!/usr/bin/env sh
        set -euo pipefail
        dnf install -y python3 pulp-cli

        "${PULP_TOOL_PATH}/e2e/post-test-cleanup.py" --config "${PULP_CLI_CONFIG}"

      volumeMounts:
        - name: cli-toml-volume
          mountPath: /etc/pulp-access
          readOnly: true
  volumes:
    - name: cli-toml-volume
      secret:
        secretName: pulp-access
        items:
          - key: cli.toml
            path: cli.toml
